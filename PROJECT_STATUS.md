# Project status

Current Q&A milestone: **Q&A Abstention Safety V1 completed with decision D — temporal / structured question failure**. On one checksum-frozen, two-source, 30-question validation, the claim-level contract reached 94.44% Evidence Recall@5, 77.78% Answer Correctness, 100% Grounded Answer, 100% Citation Precision/Recall, 0% Unsupported Claims, 100% Correct Abstention, 0% False Answers and 11.11% False Abstention. Safety improved, but frozen list/count correctness was 0/2 and temporal correctness 1/2. Ask Video remains disabled; Find Moments and Q&A retrieval are unchanged. See `ml/evaluation/QA_ABSTENTION_SAFETY_V1_RESULTS.md`.

The safety architecture extracts small deterministic question constraints, requires claim-level evidence, derives citations from validated claims, enforces explicit list counts, and safely abstains on malformed or incomplete contracts. Development used 30 new questions over two licensed sources; validation used 30 more questions over two source-disjoint sources. The original Q&A validation was not reused for tuning. Its temporal and mobile-data failures were replayed only after the new decision froze, and both safely abstained.

Current milestone: **SceneMind URL Ingestion V1 completed with decision A — passes**. Direct video URLs and supported public YouTube URLs are acquired through a provider boundary, stored locally, validated and handed to the existing durable ingest/search pipeline. URL validation rejects non-HTTP schemes, credentials and non-public IPv4/IPv6 destinations; redirects are revalidated and bounded. Size, duration, disk and timeout limits remain active. A real DirectMedia equivalence run matched upload exactly across 219.443 seconds, 44 frames, 60 Whisper segments, 44×512 visual indexes and nine frozen Visual/Speech/Smart Search Top-5 timestamp lists. A real YouTube run processed the official Blender Foundation Big Buck Bunny video into 127 frames and a searchable visual index with no residue. Production retrieval remains uncapped RRF60. See `docs/URL_INGESTION.md` and `ml/evaluation/URL_INGESTION_V1_EQUIVALENCE.md`.

Previous milestone: **Evidence-Preserving Hybrid Fusion V1 completed with decision B — improves but misses accuracy gates**. A finite eight-configuration, rank-only development search selected a one-slot-per-modality quota. On one source-disjoint validation run, useful Top-1/3/5 improved from 19/21/21 to 20/22/22 of 24 positives and MRR@5 improved from 0.8333 to 0.8681. The sole recovery was Speech; Visual did not improve and Hybrid MRR fell from 0.7500 to 0.7222. The candidate was not promoted and production remains uncapped RRF60.

Release baseline: **SceneMind v1.0.0-rc1 Portfolio Release Candidate**. The v1.0 search core is frozen: Smart Search maps directly to uncapped RRF60 Hybrid retrieval, Spoken Content maps to BM25 Speech retrieval, and Visual Content maps to CLIP/FAISS Visual retrieval over five-second frames. AUTO stays API-compatible and experimental but is absent from the normal interface. The repository has release-focused documentation and explicit version metadata. The evidence-preserving experiment changed no production algorithm or model.

Release scope is local single-operator demonstration and engineering review, not universal video understanding or public multi-tenant production. The main workflow is upload → real processing states → optional Visual/Speech index construction → natural-language search → possible timestamped moments → click-to-seek. Search-core ML experimentation is stopped for v1.0; speculative OCR, action understanding, RAG and ranking work is Future Work.

Previous milestone: Calibrated Raw-Score + Agreement Fusion V1 completed with decision **C — CALIBRATION DOES NOT GENERALIZE**. Three source-disjoint calibration videos supplied 72 queries; two further development-validation videos supplied 32 queries. Both selected mappings were worse than rank-only on source-disjoint AUC and Brier, so the experiment stopped before fusion construction or protected holdout evaluation. See `ml/evaluation/CALIBRATED_FUSION_V1_RESULTS.md`.

Previous milestone: Hybrid Ranking Failure Analysis V1 standardized 19 available ranking failures and 12 balanced success controls. Irrelevant consensus is primary in 14/19 failures, and rank-only fusion reverses a stronger same-retriever raw-score advantage in 11/19. Its diagnosis motivated the bounded calibration test above without modifying production.

The architecture diagnosis remains that fixed RRF60 cannot distinguish meaningful from incidental agreement, but no tested replacement passed independent evidence. Production therefore remains uncapped RRF60. Holdout V1 and Acceptance V2 remain evaluation-only, and there is no active v1.0 ML experiment queue.

Previous milestone: Hybrid Fusion Holdout Validation V1 completed on 2026-09-16. Two new source-disjoint Wikimedia sources and 34 retrieval-independent queries were frozen at SHA-256 `dcddffb0495cb7c397249ccd13d5818967127b6c62d01173944d0235030b64b5`. The 1.50× shared-contribution cap raises Top-1 from 12/28 to 15/28 and MRR@5 from 0.5560 to 0.6083, but lowers ALL Top-5 from 21/28 to 20/28, lowers Speech Top-5 from 8/11 to 7/11, breaks one Jimmy Wales query, rescues none, and makes one unsupported result more plausibly misleading. Decision: **REJECTED**. No second run or promotion occurred; production remains uncapped RRF60. See `ml/evaluation/HYBRID_FUSION_HOLDOUT_V1.md`.

Hybrid diagnosis: the production ranker sums unweighted RRF contributions with constant 60 and groups Speech and Visual results by the exact nearest-frame thumbnail. With both input lists capped at 50, even the weakest dual-contribution bucket scores above the strongest single-contribution bucket. Thirteen relevant Speech Top-5 and eight relevant Visual Top-5 candidates fell below Hybrid Top-5; explicit Speech reached 90.9% Top-5 on Speech queries while Hybrid reached 54.5%. This supports a bounded development experiment around overlap dominance and per-modality preservation, not an immediate production change. See `ml/evaluation/HYBRID_FUSION_DIAGNOSTICS.md`.

Hybrid refinement: three rank-only overlap caps and three independent normalized-rank variants were evaluated without using raw scores or acceptance evidence. The 1.50× cap is the only configuration that improves aggregate Top-5 and MRR without a new query failure or per-source Top-K regression. Strong-candidate retention rises from 13/28 to 14/28; positive shared-slot occupancy changes only from 89.3% to 88.6%, and Multimodal MRR dips slightly despite unchanged Top-K. A modality floor was skipped. See `ml/evaluation/HYBRID_FUSION_REFINEMENT_V1.md`.

Hybrid holdout: normal durable ingestion produced 242/304 frames and 233/498 Whisper segments for Jimmy Wales/RUN in 109.5/137.8 seconds with zero final residue. An initial Jimmy run exposed a transcript-tail boundary defect; stored segment ends are now clipped to playable duration and fully out-of-range tails removed. The final clean jobs completed on attempt one. All 28 positives had required evidence in Top-50 inputs, so all eight Cap Top-5 misses are ranking failures. Strong retention stays 18/32 and shared Top-5 occupancy stays near 90%. The failed holdout is permanently evaluation-only and must not be tuned.

Product configuration: AUTO routing was removed from the SceneMind v1.0 critical path on 2026-09-15. The normal frontend now defaults **Smart Search** directly to existing Hybrid retrieval; **Spoken Content** maps to Speech and **Visual Content** maps to Visual. AUTO remains supported by the API for compatibility and internal experiments. Retrieval algorithms are unchanged.

Human-grounded router validation: 1,669 actual five-second frames and 1,755 local Whisper segments were reviewed across ten checksum-bound CC BY/CC BY-SA videos. The source-disjoint 180/120/60 train/validation/test manifest is balanced at 120 queries per route and contains two untouched test videos. Production reaches 48.33% test accuracy. The fixed character 3-5 gram candidate reaches 60.00%, with Visual/Speech/Hybrid recall of 50.00%/40.00%/90.00% at 1.626/1.928 ms median/p95. It fails quality gates, so no second run or promotion occurs. See `ml/evaluation/HUMAN_GROUNDED_ROUTER_RESULTS.md`.
Completed: a checksum-verified CC BY 3.0 Big Buck Bunny pilot, disjoint calibration/held-out scenes, 16 reviewed query annotations, raw score/provenance reports, Recall/MRR at 1/3/5, negative false-accept and positive-abstention reporting, opt-in model-bound calibration and regression/leakage tests. Media remains ignored; no weights, embeddings or source videos are tracked.

Natural V2 adds five checksum-verified CC BY 3.0/4.0 natural videos from independent Wikimedia Commons source groups, 47 frozen annotations, complete held-out taxonomy, speech/visual/hybrid path comparisons, precision and latency, a query-path CSV, structured failures, and a version-aware regression gate. The three held-out videos never contribute to threshold fitting. Raw held-out visual R@1/3/5 is 71.4%/76.2%/85.7%; calibrated visual R@5 is 38.1%, negative FAR@5 is 10%, and positive false abstention is 52.4%. Speech R@1/3/5 is 60%/80%/80%, with 0% negative FAR and 0% positive false abstention. Hybrid raw R@5 is 77.8%; calibrated R@5 is 44.4%. See `ml/evaluation/NATURAL_V2_RESULTS.md` and `FAILURE_ANALYSIS.md`.

Verifier experiment: three additional checksum-verified Commons calibration videos add 30 frozen relation/context annotations, producing 25 positives and 21 negatives across the five-video calibration pool. BLIP ITM base COCO was tested as a batched second stage over CLIP's top five. Its calibration-only threshold 0.433838 reached 10% held-out negative FAR but repeated the current 52.4% positive false abstention, reduced held-out R@5 to 26.2%, and added 3.352 seconds median CPU latency with about 1.49 GB peak working set. It failed promotion; production code is unchanged and no second run was warranted. See `ml/evaluation/VERIFIER_RESULTS.md`.

Lightweight pair scorer experiment: two more checksum-verified Commons sources add 20 frozen phone/key/bag/cup and relation-focused calibration queries. The combined calibration pool has 66 queries across seven videos. Pinned UForm3 English small through ONNX reranks CLIP's top five in 202.1 ms warm median (229.1 ms p95), adds 163.1 MB isolated peak working set, and retains raw R@5. Its threshold reaches 0% held-out FAR but causes 47.6% positive false abstention. An eight-parameter CLIP-statistics logistic scorer takes 0.066 ms but causes 71.4% false abstention. Both fail promotion; the frozen run was not repeated and production remains unchanged. See `ml/evaluation/LIGHTWEIGHT_PAIR_SCORER_RESULTS.md`.

Object-detector experiment: a preselection inventory separates the three frozen small-object failures and shows that only the bicycle query has a top-five JPEG where every requested object is visibly present. Official Apache-2.0 YOLOX-Nano `0.1.1rc0` runs only on frozen CLIP candidates through ONNX Runtime. The calibration-only 0.8569 threshold yields 0% small-object negative FAR but 100% small-object positive false abstention. Raw candidate R@5 loss is 0 pp. Warm top-five median/p95 are 90.5/99.6 ms and added peak RSS is 58.0 MiB. It fails promotion; reranking, relationship proof, second run and production changes were skipped. See `ml/evaluation/OBJECT_DETECTOR_RESULTS.md`.

Small-object ablation: three new checksum-verified Commons sources and four frozen events separate sampled-frame visibility from detection. Exact JPEG review gives visible-evidence recall of 25%/100%/100% at 5/2/1 seconds. Frame counts grow from 47 to 116/230; combined extraction plus CLIP encoding grows from 8.00 seconds to 11.08/16.31 seconds. Fixed top-five CLIP candidate recall is only 25%/25%/50%, so denser sampling alone does not solve ranking. On the same 18 visible frames, manually target-box-reviewed recall is 66.7% for Nano 416, 83.3% for Nano 640 and 768, and 88.9% for RT-DETR-R18 640. Nano 640 has 32.5 ms p95 and 81.9 MiB added RSS; RT-DETR has 362.8 ms p95 and 552.5 MiB. Both higher-resolution Nano runs passed gates and repeated identically. Production remains unchanged; the next experiment is query-gated cached 2-second sampling with Nano 640 and wider candidates. See `ml/evaluation/SMALL_OBJECT_ABLATION_RESULTS.md`.

Bounded secondary experiment: the frozen 66-query calibration split selects top-5 coarse candidates with ±2-second windows and no spacing, averaging 9.1 secondary frames. On 31 held-out rows, bounded secondary CLIP reaches 81.0% R@5 versus 85.7% for global five-second CLIP; hypothetical global two-second CLIP reaches 76.2%. Human-reviewed held-out window recall is 50% and verified-visible secondary CLIP R@5 is 0%. Calibration selects zero detector weight. A 0.2780 no-match cutoff reaches 0% FAR but causes 81.0% overall and 100% small-object false abstention. The cache reuses 75% of 512 requests, holds 2.88 MiB, and averages 8.98 frames per routed query. Added median is 27.8 ms, p95 2.54 seconds from cold misses, and added peak RSS 84.3 MiB. Gates fail, no second run occurs, and production stays unchanged. Outcome C makes coarse candidate coverage the next task. See `ml/evaluation/BOUNDED_SECONDARY_RESULTS.md`.

Coarse candidate diversity experiment: real CLIP/FAISS retrieval over the frozen 66/31 split compares raw five/two-second candidates, temporal NMS, MMR, their combination, CLIP-change scene grouping and a bounded multi-scale diagnostic. Global two-second top five is more redundant (77.4% versus 69.0%), covers fewer temporal regions (2.94 versus 3.26), and loses three speech-label visual-path queries whose correct regions remain in top 20. Calibration selects a top-20, change-0.20, five-second-NMS multi-scale policy. It removes measured five-second-radius redundancy and preserves object/compositional/scene/action R@5, but held-out overall R@5 is 81.0% versus 85.7%, reviewed small-object recall stays 50%, speech drops 20 points and dense index growth is 2.40x. Gates fail, no second run occurs, and production stays unchanged. The two-second top-50 pool nevertheless reaches 97.6% correct-region and 100% reviewed small-object visibility recall. See `ml/evaluation/COARSE_CANDIDATE_RESULTS.md`.

Candidate-list ranking/no-match experiment: frozen five-second CLIP top-20/top-50 lists receive score/rank/distribution, temporal-support, embedding-agreement and visual-change features. Source-held-out calibration selects a 17-parameter logistic scorer, but its held-out R@5 is 76.2% versus the 85.7% raw baseline; top-50 Oracle is 100%. The query-level threshold reaches 0% held-out FAR by falsely abstaining on 90.5% of positives. Both quality gates fail although 0.44/0.96 ms median/p95 and 232 bytes of parameters pass resources. Existing explicit channel routing with real BM25 evidence reaches 95.2% R@5, so outcome E identifies routing as dominant. No second run or production change occurs. See `ml/evaluation/CANDIDATE_LIST_RANKING_RESULTS.md`.

Query routing: three new source-disjoint Commons lecture/tutorial/demo videos contribute 961.3 seconds, 205 real Whisper-tiny segments, and 36 frozen balanced route queries. A deterministic heuristic and a 54-parameter class-balanced softmax router use only readable query-text features. Learned AUTO reaches 96.8% held-out routing accuracy and R@1/3/5 of 85.7%/92.9%/95.2%, matching explicit-route Oracle R@5/MRR with no category loss. Median/p95 routing is 0.031/0.042 ms and the compact artifact is 2,077 bytes. The second identical frozen evaluation matches. AUTO is integrated behind a default-enabled flag; Visual, Speech, and Hybrid overrides remain. See `ml/evaluation/QUERY_ROUTING_RESULTS.md`.

Personal acceptance: 54 queries over the three exact user-supplied videos were frozen before retrieval and then run through production ingestion, CLIP, Whisper, BM25/RRF, AUTO, and every explicit mode. AUTO routing is 77.8%; positive-query useful Top-1/3/5 is 52.8%/66.7%/83.3%. English Top-5 is 88.9% versus 77.8% for Turkish, and every one of the 12 routing errors is Turkish. Only 22.2% of 18 negatives avoid a misleading response. AUTO search remains fast at 23.25/32.02 ms median/p95. The original 419 MiB tutorial is rejected by the 250 MiB cap and requires an external five-minute transcode; it is also silent and only 22:49, while the demo is 11:12 and the street smoke clip is 35 seconds. The suite therefore does not validate 30–60 minute use. See `ml/evaluation/PERSONAL_ACCEPTANCE_RESULTS.md`.

Measured held-out results (four positives, four negatives): Recall@1=0.375, Recall@3=0.875, Recall@5=1.0; MRR@1=0.75 and MRR@3/5=0.8333. Calibration reduced false accepts from 4/4 to 2/4 with no observed positive-query abstention. The threshold was fitted only on the other scene. Two real-model runs matched quality metrics and passed the regression gate. This small single-film animated pilot is not broad natural-video quality; no speech/hybrid accuracy claim. See ml/evaluation/PILOT_RESULTS.md and reports/ for exact measurements. Synthetic rerun: both positives pass at K=1; its one negative still returns a neighbor.

Turkish compatibility: seven legally reusable videos form six source-disjoint groups with 42 calibration and 30 held-out natural Turkish queries. Production routing is 50.0%/53.3% on calibration/held-out; the selected cheap router reaches 88.1% source-leave-one-out calibration and 76.7% held-out. Cheap lexical adaptation reaches held-out forced Visual/Speech/Hybrid R@5 of 86.7%/87.5%/71.4% and AUTO R@5 of 80.0%. Direct Turkish Visual R@5 is stronger at 93.3%. A pinned multilingual MiniLM diagnostic reaches Speech/Hybrid/AUTO R@5 of 100%/85.7%/86.7%, but adds 254 MiB RSS, about 458 MiB cache and 9.995/14.378 ms median/p95 query latency without fixing routing. Outcome E: quality gates fail, production remains unchanged, no second run occurs, and frozen personal acceptance is not rerun. See `ml/evaluation/TURKISH_COMPATIBILITY_RESULTS.md`.

Local hardening: optional shared-operator Basic/Bearer authentication, browser credential support, mutation Origin checks, SQL-backed durable queue, isolated persistent inference child, model reuse, attempt/backoff persistence, job listing/manual retry, process-tree deadlines, parent-death monitoring, single-host worker/execution locks, atomic queue capacity and duplicate protection. Existing upload byte/duration/resolution limits are retained. Serialized migrations support SQLite and PostgreSQL. Docker validation uses non-root Linux and a disposable internal PostgreSQL network; optional Compose stays loopback-only.

Long-video infrastructure: upload now defaults to 1 GiB and 60 minutes and remains configurable. Request chunks write directly to one source file; disk reserve plus processing headroom is checked before enqueue. Frames and embeddings promote atomically, temporary audio is removed, and the supervisor cleans stage partials after timeout/failure. The unchanged 439,295,727-byte tutorial completed ingest and CLIP indexing in 121.3 seconds with 274 frames and 444,845,520 bytes persistent storage. A 45-minute audio stress fixture completed ingest, Whisper and CLIP in 245.4 seconds with 540 frames, 373 valid ordered ASR segments, 3.02 GB peak worker-tree RSS, 1.56 GB steady worker-tree RSS and zero temporary bytes. This is infrastructure evidence, not long-video search-quality evidence. See `ml/evaluation/LONG_VIDEO_INGEST_RESULTS.md`.

English path-aware no-match: six source-disjoint Commons groups provide 72 balanced calibration and 48 balanced held-out queries across Visual, Speech and Hybrid. AUTO routing passes at 95.83%, and rule latency is 0.0019/0.0021 ms median/p95 with no model memory. Quality fails: held-out Visual/Speech/Hybrid FAR is 8.33%/33.33%/50.00%, false abstention is 83.33%/16.67%/16.67%, and overall R@5 falls from 87.50% to 41.67%. Outcome E stops rejection-model experimentation for v1.0. No second run, personal rerun, feature flag, or production change occurred. See `ml/evaluation/PATH_AWARE_NO_MATCH_RESULTS.md`.

Conservative product UX: search labels candidates as "Most relevant moments," explains relevance uncertainty once per result list, hides raw ML scores, retains timestamps/transcript excerpts/evidence and click-to-seek, and avoids declaring that missing content exists or is absent. Smart Search / Hybrid is now the frontend default, with Spoken Content and Visual Content as explicit alternatives. AUTO remains an API-compatible experimental path. The initial local inventory found no eligible source and produced the historical preparation decision D; eligible media was supplied afterward. See `ml/evaluation/FINAL_ENGLISH_ACCEPTANCE_PREPARATION.md` for that superseded inventory result.

Final English acceptance: a new CC BY 4.0, real continuous 54:11 technical presentation was independently reviewed and bound to a 30-query manifest before product search. Normal durable ingestion completed in 579.258 seconds with 651 frames, 684 English ASR segments, 2.80 GiB peak worker-tree RSS, 555.4 MiB peak API-tree RSS, 285.6 MiB persistent application data, no retry, no failure, and no temporary residue. AUTO routing is 56.67%; positive useful Top-1/3/5 is 50.00%/65.38%/65.38%, MRR@5 is 0.5705, and outcomes are 12 PASS / 5 PARTIAL / 9 FAIL. Visual Top-5 is 83.33% and Compositional Top-5 is 100%, while Speech Top-5 is 30%. All four negatives remain understandable under conservative wording. Six weak AUTO Speech/Hybrid cases are recovered by their frozen explicit route, identifying AUTO routing generalization as the dominant blocker. Production behavior is unchanged. See `ml/evaluation/FINAL_ENGLISH_ACCEPTANCE_RESULTS.md`.

Final English Acceptance V2: a new source-disjoint CC BY-SA 4.0, real continuous 30:29 English presentation was fully reviewed before a checksum-bound 30-query manifest was frozen. Normal durable processing completed in 244.393 seconds with 366 frames, 576 English ASR segments, 1.94 GiB peak worker-tree RSS, 562.77 MiB peak search API-tree RSS, 166.60 MiB persistent data, zero retry/failure/residue, and no production change. Smart Search / Hybrid reaches PASS-level Top-1/3/5 of 76.92%/76.92%/84.62%, MRR@5 0.7865, and 22 PASS / 1 PARTIAL / 3 FAIL. All eight multimodal queries pass at rank 1; Speech Top-5 is 70.00%. Explicit Speech recovers both primary Speech FAILs at rank 1; the sole PARTIAL also moves to rank 1 but remains incomplete. Hybrid fusion/ranking is the dominant blocker. All four negatives retain conservative UX. See `ml/evaluation/FINAL_ENGLISH_ACCEPTANCE_V2_RESULTS.md`.

English router generalization: 450 independently authored queries across 15 source scenarios are balanced 150/150/150 Visual/Speech/Hybrid and split by source into 270 train, 90 validation and 90 frozen test. The unchanged 54-parameter router reaches 50.00% accuracy, with 13.33% Speech and 36.67% Hybrid recall. Validation selects a character 3–5 gram TF-IDF linear classifier; with its validation-selected Hybrid fallback it reaches 95.56% accuracy and 100.00%/90.00%/96.67% route recall at 2.7706/3.3677 ms median/p95. The 731,122-byte experimental artifact uses about 289 KB model/vocabulary memory. Fallback lowers frozen accuracy from 97.78% to 95.56%. The passing second run is deterministic and all 30 protected acceptance queries were checked with no exact overlap. Decision E keeps production unchanged because source cards were not verified against real video evidence. See `ml/evaluation/ENGLISH_ROUTER_GENERALIZATION_RESULTS.md`.

Executed checks: 227 backend tests passed with one opt-in real-network skip and two upstream deprecation warnings. Ruff lint and changed-file formatting, frontend ESLint, TypeScript, production build, and 14 desktop/mobile Playwright flows passed; two opt-in real-model Playwright cases skipped. A focused frozen retrieval/Q&A regression run passed 44 tests.

Limits: one host/operator, no tenant ownership/RBAC, distributed workers, high availability or public deployment validation. API query inference remains in-process without a worker deadline. Delivery is at-least-once. A crash before enqueue may leave an unqueued upload asset; never silently delete user media. Inline mode preserves V1 limitations unless durable mode is enabled. Containerized real ML inference remains unverified. Natural V2 is a small diagnostic set, not a population estimate; pretraining overlap is unknown and timestamp boundaries have no tolerance. OpenCV VFR duration is approximate. Calibration remains opt-in and transfers poorly across the measured domains. The evaluation used a configured OpenAI API provider; no training, OCR, action recognition or multimodal Video RAG was added.

Next milestone: Q&A Structured-Question Evidence V1 using only new development sources. Represent list completeness and temporal anchors without changing frozen BM25 Top-5 retrieval, tuning either Q&A validation set, or enabling Ask Video.
